package cr.ac.una.glifo.core.sync

import javax.inject.Inject
import javax.inject.Singleton

@Singleton
class SyncQueue @Inject constructor() {
    // Manages the offline capture queue and synchronization
}
